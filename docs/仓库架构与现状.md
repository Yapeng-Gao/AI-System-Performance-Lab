# 仓库架构与现状

> 读本文可快速判断：**该改哪里**。L0 全景与模块弧已锁，见 [`CUDA专栏规划.md`](CUDA专栏规划.md) §2–§4。Module A ✅；Module B ✅；C-01～C-06 ✅。

---

## 1. 当前目录（已瘦身）

```text
AI-System-Performance-Lab/
├── article/                 # 专栏正文 + assets（封面 / 实测图）
│   ├── 01_cuda_basic/       # 导读 + Module A：A-01～A-10
│   ├── 02_memory_optim/     # Module B：B-01～B-10
│   └── 03_compute_primitives/  # Module C：C-01…
├── examples/                # 章节 .cu + profile/dump 脚本（CMake 主产物）
│   ├── 01_cuda_basics/
│   ├── 02_memory_optim/
│   └── 03_compute_primitives/
├── docs/
│   ├── CUDA专栏规划.md      # 进度导航（总表）
│   ├── CUDA专栏大纲/        # 按章写作大纲分册
│   ├── 仓库架构与现状.md
│   ├── results/             # 实测摘要 + CSV
│   └── *专栏规划.md         # 远期大纲（非实现主线）
├── scripts/                 # plot_b0N_*.py / plot_c0N_*.py / NCU 辅助
├── cmake/                   # NVCCFlags 等
├── CMakeLists.txt           # 只 add_subdirectory(examples)
└── build/ 或 cmake-build-*  # 构建产物（gitignore）
```

**运行闭环（B-01～B-09、C-01～C-06 已齐；B-10 / 日后 C-10 为索引）**

1. `examples/<module>/0N_*.cu` → `build/bin/` 或 `cmake-build-debug/bin/`  
2. 同目录 profile / dump 脚本（可选）  
3. `docs/results/<章>_*.csv` + `*.md`  
4. `python scripts/plot_*.py` → `article/<module>/assets/`  
5. 正文：原理短 ASCII，实测 matplotlib

---

## 2. 已删除的占位目录

下列路径曾存在于仓库，**已移除**（空壳 / 与专栏主线重复）：

| 原路径 | 原因 |
|--------|------|
| `src/`、`include/` | 占位 `aspl_core`，示例不依赖 |
| `benchmarks/` | 与 `examples/02_memory_optim` 重复骨架 |
| `python/`、`tests/` | Module E / 单测空壳 |
| `docker/`、`third_party/` | 仅 README |
| 早先空壳 `examples/03_*` / `04_*` | 曾仅 README；**现** `03_compute_primitives` 已随 C-01 正式落地 |

若以后做算子库 / Python 绑定 / 独立 bench suite，再按需新建，不必提前占坑。

---

## 3. 专栏进度

| 模块 | 文章 | 示例 | 备注 |
|------|------|------|------|
| 导读 | ✅ | — | `article/01_cuda_basic/00. 专栏导读*`；已交付 vs 规划分栏 |
| A（1–10） | ✅ | ✅ | 教法弧收束于 A-10 Roofline |
| B（B-01～B-10） | ✅ | ✅（B-10 无新 `.cu`） | B-01～B-10 已收口；B-04 hint ~1× |
| C（C-01～） | 🟡 | 🟡 | C-01～C-06 ✅；后半候选，不阻塞 |
| D / E | ⏳ | — | 章表在 `docs/CUDA专栏大纲/Module-D.md` / `Module-E.md` |

进度导航：[`CUDA专栏规划.md`](CUDA专栏规划.md)。

---

## 4. 正文插图约定

- **专栏/模块全景**：权威构图是规划 §2 ASCII；日后一张本地总图挂导读。L1 弧在 `CUDA专栏大纲/Module-*.md`  
- **章内原理 / 演进**：本地原理图（1～2）或短 **ASCII**；禁止 csdn 外链当主图  
- **实测**：`docs/results/*.csv` + `scripts/plot_*.py` → PNG  
- **封面**：`article/**/assets/*-cover.png`  
- **不做**：信息过载的 AI 教学海报冒充原理图
