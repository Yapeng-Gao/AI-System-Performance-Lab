# 仓库架构与现状

> 读本文可快速判断：**该改哪里**。主线是 CUDA 专栏 Module A/B。

---

## 1. 当前目录（已瘦身）

```text
AI-System-Performance-Lab/
├── article/                 # 专栏正文 + assets（封面 / 实测图）
│   ├── 01_cuda_basic/       # Module A：A-01～A-10
│   └── 02_memory_optim/     # Module B：B-01～B-07
├── examples/                # 章节 .cu + profile/dump 脚本（CMake 主产物）
│   ├── 01_cuda_basics/
│   └── 02_memory_optim/
├── docs/
│   ├── CUDA专栏规划.md
│   ├── 仓库架构与现状.md
│   ├── results/             # 实测摘要 + CSV
│   └── *专栏规划.md         # 远期大纲（非实现主线）
├── scripts/                 # plot_b0N_*.py / NCU / dump_sass 辅助
├── cmake/                   # NVCCFlags 等
├── CMakeLists.txt           # 只 add_subdirectory(examples)
└── build/ 或 cmake-build-*  # 构建产物（gitignore）
```

**运行闭环（B-05～B-07）**

1. `examples/02_memory_optim/0N_*.cu` → `build/bin/` 或 `cmake-build-debug/bin/`  
2. 同目录 profile / dump 脚本（可选）  
3. `docs/results/B-0N_*.csv` + `*.md`  
4. `python scripts/plot_b0N_*.py` → `article/02_memory_optim/assets/`  
5. 正文：原理短 ASCII，实测 matplotlib

---

## 2. 已删除的占位目录

下列路径曾存在于仓库，**已移除**（空壳 / 与专栏主线重复）：

| 原路径 | 原因 |
|--------|------|
| `src/`、`include/` | 占位 `aspl_core`，示例不依赖 |
| `benchmarks/` | 与 `examples/02_memory_optim` 重复骨架 |
| `python/`、`tests/` | Module E / 单测空壳 |
| `docker/`、`third_party/` | 仅 README |
| `examples/03_*`、`04_*` | Module C/E 空 README |

若以后做算子库 / Python 绑定 / 独立 bench suite，再按需新建，不必提前占坑。

---

## 3. 专栏进度

| 模块 | 文章 | 示例 | 备注 |
|------|------|------|------|
| A（1–10） | ✅ | ✅ | 封面已加；部分正文仍有 CSDN 外链图 |
| B（B-01～B-07） | ✅ | ✅ | B-05～B-07：ASCII + 可复现实测图 |
| B（B-08） | 🟡 正文+示例 ✅；实测 ⏳ | 🟡 `08_tma_intro.cu` ✅ | TMA；结果表待 sm_90+ |
| B（B-09～B-10） | ⏳ | ⏳ | Layout / Checklist |
| C / D / E | ⏳ | — | 仅规划文档，无代码目录 |

---

## 4. 正文插图约定

- **原理 / 时间线 / 路径**：短 **ASCII**  
- **实测**：`docs/results/*.csv` + `scripts/plot_b0N_*.py` → PNG  
- **封面**：`article/**/assets/*-cover.png`  
- **不做**：信息过载的 AI 教学海报当正文原理图
