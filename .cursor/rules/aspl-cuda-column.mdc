---
description: ASPL CUDA 专栏硬约束——全景主线已锁；写稿/补实测/改 examples 先走 aspl-cuda-article；commit 前同步导航
globs: article/**/*.md,docs/CUDA*/**,docs/results/**,docs/仓库架构与现状.md,examples/**/*.cu,examples/**/*.sh,examples/**/README.md,scripts/plot_*.py,README.md
alwaysApply: false
---

# ASPL CUDA 专栏（精简硬约束）

完整流程见 skill：`.cursor/skills/aspl-cuda-article/SKILL.md`。命中本规则文件时**先读该 skill 再动手**。

## 必须遵守

0. **全景主线已锁定。** 权威是 `docs/CUDA专栏规划.md` **§2 L0 + §2.2 展开权 + §3 布局 + §4 教法弧**。改正文 / 大纲 / `.cu` / 导航都必须落在这条线上：A 映射度量 → B 形状→on-chip→跨空间→引擎→布局 → C 通信→争用同步→摊销 launch → D 规约→矩阵→epilogue → E 可调用→可测量→可交付。禁止改弧、抢展开权、另起叙事（例如 B-01/B-02 开篇主讲 TMA）、并列第二条「当前焦点」。**唯一例外**：用户明确说「优化全景 / 改全景图 / 重锁 L0」。否则先钩子、后展开，细节回指定章。
1. **主证据** = 裸跑 CUDA event **median**；禁止把 `ncu` 附着时程序自打印的 ms/GB/s 当结论。
2. **用户一贴** sweep / NCU / SASS 输出 → 立刻落 `docs/results/` 并更新正文对应节（不要只口头解读）。
3. **默认不加** `*_profile_*.sh`；用户明确要求批量 NCU/NSYS/SASS 才加。NCU 过滤：`--kernel-name-base function|demangled|mangled` + `--kernel-name regex:…`（不要把 kernel 名塞进 `-base`）。
4. **新章**先大纲（`docs/CUDA专栏大纲/` 的 `B-0N_*` / `C-0N_*`）并等用户确认；局部任务（补数/改图/改 TL;DR）不重开全流程。
5. **§7 扩展阅读**与 **§10 参考文献**去重：§7 ≤4 条出口；完整分层在 §10（若章节编号偏移，以正文参考文献节为准）。
6. 带宽/布局章：用 **useful payload**；提醒 L2 可能抬高绝对 GB/s，主看加速比形状。
7. **不擅自 commit/push**；用户明确要求再提交。Commit 时跳过仅换行符噪声的大纲文件。
8. **Commit 前同步相关文档**（与本次改动同批进暂存区；禁止只交 `.cu`/正文而导航仍写「待测」）：
   - `docs/CUDA专栏规划.md`：该章状态 ✅/🟡 + 大纲链接；「当前焦点」指向下一章
   - 该章大纲：顶部状态、本机要点、交付 checklist
   - `docs/CUDA专栏大纲/Module-*.md` 与 `docs/CUDA专栏大纲/README.md`：章节表状态
   - 根 `README.md`：主线一句 + 专栏映射表 + 必要时运行/plot 示例
   - `examples/<module>/README.md`：新 `.cu` 入口行
   - `docs/仓库架构与现状.md`：目录树 / 进度表若已过时则改
   - 有实测：`docs/results/<章>_*` 与正文数字一致
9. 不要把 Host CE、设备 `memcpy_async`、TMA bulk 混成一层；不要默认每章都要 sm_90+。
10. Checklist / 收束章可无新 `.cu`（如 B-10）；Module C 起默认仍要 micro-bench，除非该章大纲裁决不做。
11. 新 Module 目录随**首章落地**再建；禁止提前建空 `examples/03_*` README。
12. **文首和文末**都要 GitHub `https` 绝对链：仓库 + Star；有 `.cu` 则 blob；下一篇用 `tree/main/article/<module>/`（目录，禁止 `./A-xx.md`）。句式见 `.cursor/skills/aspl-cuda-article/article-template.md`。
13. 用户说「优化」= 改正文（事实、CTA、外链、文风）。**默认不压 PNG**；未要求不把体积当任务。图禁 `csdnimg`，用本地 `assets/`。用户说重画：按正文结论新画（简单、少字），不必还原旧图结构。
14. **机制节写够本章概念**（是什么 / 为什么 / 怎么发生）。TL;DR 和边界表不能替代讲解；只有邻章已有完整正文才「去 A-xx」。

## 收口一句话

结论有数、边界清晰、主命令一条、文首文末绝对链齐、旁证已落盘（若做了）、导航文档已同步 → 能停则停；用户要 commit 时把上述文档一并纳入。PNG 体积不挡发布。
